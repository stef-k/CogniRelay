[Unit]
Description=CogniRelay Ops Job: metrics.poll_and_alarm_eval
After=cognirelay.service
Wants=cognirelay.service

[Service]
Type=oneshot
User=cognirelay
Group=cognirelay
ExecStart=/usr/local/bin/cognirelay-ops-run.sh metrics.poll_and_alarm_eval
